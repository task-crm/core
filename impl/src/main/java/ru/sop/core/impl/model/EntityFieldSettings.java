package ru.sop.core.impl.model;

import java.util.UUID;

/**
 * В этом классе содержатся настройки для полей сущности {@link EntityField}
 */
public record EntityFieldSettings(
    UUID fieldId
) {
}
